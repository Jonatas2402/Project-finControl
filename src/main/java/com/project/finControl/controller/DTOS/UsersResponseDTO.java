package com.project.finControl.controller.DTOS;

//Response = O que será devolvido como resposta.
public record UsersResponseDTO(Long id,
                               String name,
                               String email) {
}
